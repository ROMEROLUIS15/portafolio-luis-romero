# Environment — NEVER commit real secrets
.env
.env.local
.env.*.local
supabase/functions/chat/.env.local

# Claude Code local settings. Approved shell commands are recorded verbatim in
# the allow-list, so any credential passed on a command line ends up in here.
# .claude/settings.json (shared, no secrets) stays tracked.
.claude/settings.local.json

# Dependencies
node_modules/
package-lock.json

# Coverage / build artifacts
coverage/
dist/

# CV — personal documents, not for version control
LuisRomero_CV_ATS_EN.md
LuisRomero_CV_ATS_ES.md
